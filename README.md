# CSE-0611328-Computer-Graphics
# Computer Graphics Lab Assignment

**Student Name:** Sourav Debnath  
**Student ID:** 0432410005101013  
**Environment:** Linux (Debian) / VS Code  

---

## 📌 Assignment Objective

1. Create an OpenGL window titled with the author's full name (**Sourav Debnath**).
2. Set the window background color to **Magenta** (`RGB: 1.0, 0.0, 1.0`).
3. Implement a keyboard callback mechanism that closes the window upon pressing the **'S'** key (the first letter of "Sourav").
4. Provide verifiable proof of local compilation and execution containing the author's username in the terminal prompt path.

---

## 🛠️ Requirements & Dependencies

To build and run this project, the following dependencies must be installed on Debian/Ubuntu systems:

- `build-essential` (GCC / G++)
- `libglfw3-dev`
- `libgl1-mesa-dev`
- GLAD OpenGL Loader

Install missing packages via:
```bash
sudo apt update && sudo apt install build-essential libglfw3-dev libgl1-mesa-dev
